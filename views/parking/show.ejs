<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    Address:<%= parking[0].address %> <br>
    Total Slots:<%= parking[0].totalSlots %>  <br>
    Available Slots: <%= parking[0].availableSlots%> <br>
   
    <% if(parking[0].availableSlots>0){%>
    <button id="btn">BOOK NOW</button>
    <script>
        const btn=document.getElementById("btn");
        btn.addEventListener('click',function (){
             window.location.replace("http://localhost:3000/user/parking-slot-booking/<%= parking[0]._id %>");
        });
    </script>
    <%}else{%>
    <p> There are no available slots</p>
    <%}%>
</body>
</html>